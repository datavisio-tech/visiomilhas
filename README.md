# VisioMilhas - MVP1

Scaffold inicial do projeto VisioMilhas (Next.js App Router, TypeScript, Tailwind).

Passos iniciais:

1. Preencher `.env.local` localmente com as variáveis do `.env.example`.
2. Rodar `npm install`.
3. Rodar `npm run dev`.
